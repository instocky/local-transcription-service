# DEPRECATED
#
# This plist was the Phase-D user LaunchAgent template.
# Bootstrap failed with exit 125 because <key>UserName</key> is
# FORBIDDEN in per-user LaunchAgents (~/Library/LaunchAgents/).
#
# Use `local.local-transcription-service.plist` in this directory
# instead — it is a system LaunchDaemon that mirrors
# /Library/LaunchDaemons/local.litellm.plist on this Mac Mini.
#
# Install instructions:
#   sudo cp scripts/launchd/local.local-transcription-service.plist /Library/LaunchDaemons/
#   sudo chown root:wheel /Library/LaunchDaemons/local.local-transcription-service.plist
#   sudo chmod 644 /Library/LaunchDaemons/local.local-transcription-service.plist
#   sudo launchctl bootstrap system /Library/LaunchDaemons/local.local-transcription-service.plist
#
# See README.md §"Deploying" for the full Mac Mini workflow.
